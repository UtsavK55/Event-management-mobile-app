import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  image: {
    height: 100,
    width: 350,
    alignSelf:'center',
    marginTop: 60,
    marginBottom: 20
  },
  buttonContainer:{
    marginTop:4
  }
});
